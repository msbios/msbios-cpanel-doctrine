{
  "name": "msbios/cpanel-doctrine",
  "description": "MSBios CPanel Doctrine",
  "license": "GPL",
  "keywords": [
    "php"
  ],
  "homepage": "https://github.com/msbios/msbios-cpanel-doctrine",
  "minimum-stability": "stable",
  "authors": [
    {
      "name": "Judzhin Miles",
      "email": "info@msbios.com"
    }
  ],
  "autoload": {
    "psr-4": {
      "MSBios\\CPanel\\Doctrine\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "MSBiosTest\\CPanel\\Doctrine\\": "tests/"
    }
  },
  "require": {
  },
  "require-dev": {
    "msbios/test": "^1.0",
    "msbios/debug": "^1.0"
  },
  "scripts": {
    "cs-check": "phpcs",
    "cs-fix": "phpcbf",
    "test": [
      "@cs-check",
      "phpunit --colors=always"
    ],
    "test-coverage": "phpunit --colors=always --coverage-clover clover.xml",
    "upload-coverage": "coveralls -v",
    "alter": "./vendor/bin/doctrine-module orm:schema-tool:update --force",
    "themes": [
      "cd bin && ln -s ../vendor/msbios/cpanel/themes/limitless/public/ limitless",
      "cd bin && ln -s ../vendor/msbios/application/themes/default/public/ default"
    ],
    "serve": "php -S 0.0.0.0:3107 -t bin bin/msbios-cpanel-doctrine-module.php",
    "commit": "git add . && git commit -a -m \"Lazy Composer Commiter\" && git push"
  }
}
